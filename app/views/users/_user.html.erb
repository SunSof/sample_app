<ul class="mb-3">
  <%= gravatar_for user %> 
  <%= link_to user.name, user, class:"text-dark"%>
  <% if current_user.admin? && !current_user?(user) %>
   <%= button_to "Delete", user, method: :delete, data: {confirm: "Are you sure?"}, class:"btn btn-outline-danger mt-2" %>
  <% end %>
</ul>